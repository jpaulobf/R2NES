package com.nesemu.rom;

/**
 * Interface for the iNES header.
 * This interface defines the structure of the iNES header,
 * which includes information about the ROM such as the number of banks,
 * flags, mapper type, and other metadata.
 */
public class INesHeader {
    // TODO: atributos do cabeçalho iNES (número de bancos, flags, mapper, etc.)
}
